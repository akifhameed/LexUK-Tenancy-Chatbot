# LexUK runtime + tooling dependencies.
# Pinned with >= so minor updates flow in but no surprises from majors.

# === Core LLM + RAG ===
openai>=1.50.0
chromadb>=0.5.0
tenacity>=9.0.0

# === Configuration & validation ===
pydantic>=2.9.0
pydantic-settings>=2.5.0
python-dotenv>=1.0.1

# === UI ===
# Pin a recent Gradio 5.x that uses the new huggingface_hub auth API
# (older 5.0.x imports the removed `HfFolder` symbol and fails to start).
gradio>=5.30.0,<6.0.0
# Defensive cap on huggingface_hub - any version old enough still has
# HfFolder, in case Gradio's own pin is loose. Belt + braces.
huggingface_hub>=0.25,<1.0
# audioop-lts: backport of Python's `audioop` module for Python 3.13+
# (removed from stdlib in 3.13). No-op on Python 3.12 (the pinned version).
audioop-lts; python_version >= "3.13"

# === Evaluation & reporting ===
numpy>=1.26.0
pandas>=2.2.0
matplotlib>=3.9.0
plotly>=5.24.0
tqdm>=4.66.0
